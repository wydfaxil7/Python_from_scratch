import shutil
import os

# shutil.copy("main.py", "main2.py")               #copies a file and creates a new one with the same content
# shutil.copytree("Tutorial", "HEXADEXA")          #copies a folder and all it content/files inside
# shutil.move("Tutorial/main2.py", "main2.py")       #moves a file to a different location")
shutil.rmtree("HEXADEXA")